spring.application.name=JavaReactProject

# PostgreSQL Database Configuration
spring.datasource.url=hfhhfhfhf
spring.datasource.username=acbdhjf
spring.datasource.password=hghhghgh
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

jwt.access-token-secret=fffff
jwt.refresh-token-secret=yhfhhfhf
jwt.access-token-expires=6h
jwt.refresh-token-expires=24h
app.allowed.origins=origins
jwt.access-token-secret=your key
